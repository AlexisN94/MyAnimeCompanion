package com.alexis.myanimecompanion

import org.junit.runner.RunWith
import org.robolectric.RobolectricTestRunner

@RunWith(RobolectricTestRunner::class)
class TokenStorageManagerTest {
//    lateinit var tokenStorageManager: TokenStorageManager
//    lateinit var token: DomainToken
//
//    @Before
//    fun setup() {
//        val context = ApplicationProvider.getApplicationContext<Context>()
//        tokenStorageManager = TokenStorageManager.getInstance(context)
//        token = DomainToken("accessToken", 1L, "refreshToken", "tokenType")
//    }
//
//    @Test
//    fun `TokenStorageManager should write and read to SharedPreferences`() {
//        tokenStorageManager.setToken(token)
//        val token = tokenStorageManager.getToken()
//        Assert.assertEquals(token, this.token)
//    }
}
